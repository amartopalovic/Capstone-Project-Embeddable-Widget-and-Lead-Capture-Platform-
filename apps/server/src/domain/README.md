# Domain rules

Pure business rules with no infrastructure or framework dependencies.

**Status:** intentionally empty in Stage 1. The first domain rules (tenancy invariants and shared
validation) arrive in Stage 2.
