# @lcp/web

React platform, public pages, and dashboard.

**Status:** Stage 1 skeleton. It boots and builds with a single placeholder view. Real pages arrive
from Stage 3 (auth) through Stage 12 (public site).

Runs on port **5173**, a different origin from `@lcp/demo` on 5174.
