# @lcp/widget-runtime

Framework-free TypeScript widget loader and runtime build.

**Status:** empty workspace shell created in Stage 1 with a browser library build, to prove this
bundle is produced independently of the React application. The real loader, Shadow DOM runtime,
and CI bundle-size budgets arrive in Stage 6.

This workspace must never gain a dashboard framework dependency (blueprint section 8.3).
