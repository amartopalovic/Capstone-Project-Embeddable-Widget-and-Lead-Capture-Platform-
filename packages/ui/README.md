# @lcp/ui

Shared accessible React components and design tokens.

**Status:** empty workspace shell created in Stage 1 to establish the boundary, build, and
type-check wiring. Design tokens and accessible primitives arrive with the first real UI in
Stage 5; the WCAG 2.2 AA audit is Stage 13.
